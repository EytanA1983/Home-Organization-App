# ML Service Requirements
# Heavy ML dependencies - kept separate from main backend

# FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# PyTorch (CPU version - adjust for GPU if needed)
# For CPU: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# For GPU: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Transformers (for BERT, etc.)
transformers>=4.35.0
sentencepiece>=0.1.99
protobuf>=4.25.0

# Optional: TensorFlow (if using TensorFlow models)
# tensorflow>=2.13.0

# Optional: scikit-learn (for traditional ML)
# scikit-learn>=1.3.0

# Optional: NumPy, Pandas
numpy>=1.24.0
pandas>=2.0.0

# Optional: ONNX Runtime (for optimized inference)
# onnxruntime>=1.16.0
