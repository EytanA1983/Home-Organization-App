# הוספת תמונות רקע לחדרים

## 📸 הוראות:

### 1. הוסף את התמונות:
העתק את תמונות הרקע לתיקייה: `frontend/public/`

#### תמונות נדרשות:
- **חדר אמבטיה:** `bathroom-background.jpg`
- **חדר שינה:** `bedroom-background.jpg`

### 2. דרישות תמונות:
- **גודל מומלץ:** 1920x1080px או יותר
- **פורמט:** JPG או PNG
- **איכות:** גבוהה (התמונה תהיה דהויה, אז איכות טובה חשובה)

### 3. הקוד כבר מוכן:
- `RoomPage.tsx` מזהה אוטומטית חדר אמבטיה וחדר שינה
- מציג את התמונה כרקע דהוי (opacity: 0.3)
- מוסיף overlay עדין להדגשת הדהייה
- כרטיסיות המשימות נראות טוב על הרקע

### 4. זיהוי חדרים:
הקוד מזהה חדרים לפי:

**חדר אמבטיה:**
- "שירותים"
- "bathroom"
- "אמבטיה"

**חדר שינה:**
- "שינה"
- "bedroom"
- "חדר שינה"

### 5. אם התמונה לא נטענת:
- ודא שהקובץ נמצא ב-`frontend/public/`
- ודא ששם הקובץ נכון: `bathroom-background.jpg` או `bedroom-background.jpg`
- נסה לרענן את הדף (Ctrl+F5)
- בדוק את הקונסול לשגיאות

## 🎨 אפשרויות נוספות:

### הוספת תמונות לחדרים נוספים:
אם תרצה להוסיף תמונות רקע לחדרים נוספים:

1. הוסף את התמונות ל-`frontend/public/`
2. עדכן את `RoomPage.tsx`:
   ```typescript
   // הוסף פונקציית זיהוי חדר
   const isKitchen = (roomName: string): boolean => {
     const name = roomName.toLowerCase();
     return name.includes('מטבח') || name.includes('kitchen');
   };

   // הוסף סגנון רקע
   const kitchenBackgroundStyle: React.CSSProperties = {
     backgroundImage: 'url(/kitchen-background.jpg)',
     backgroundSize: 'cover',
     backgroundPosition: 'center',
     backgroundRepeat: 'no-repeat',
     opacity: 0.3,
   };

   // עדכן את hasBackgroundImage ו-backgroundStyle
   ```

## 📝 דוגמה למבנה קבצים:
```
frontend/public/
  ├── bathroom-background.jpg
  ├── bedroom-background.jpg
  ├── kitchen-background.jpg (אופציונלי)
  └── ...
```

## 🎨 התאמת רמת הדהייה:
אם תרצה לשנות את רמת הדהייה, שנה את הערך `opacity`:
- `0.2` - דהוי מאוד
- `0.3` - דהוי (ברירת מחדל)
- `0.4` - פחות דהוי
- `0.5` - עוד פחות דהוי

## ✨ התוצאה:
- תמונת רקע דהויה מאוד (opacity 0.3)
- Overlay עדין להדגשת הדהייה
- טקסט קריא
- כרטיסיות משימות נראות טוב
- עיצוב נקי ומינימליסטי
